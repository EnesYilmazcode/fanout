{
  "name": "fanout",
  "version": "0.1.0",
  "private": true,
  "description": "Personal AI capacity router — one endpoint, all your keys, automatic failover.",
  "scripts": {
    "secrets": "node scripts/gen-secrets.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsx test/smoke.mts",
    "test:upstash": "tsx test/upstash.mts",
    "test:e2e": "tsx test/e2e.mts",
    "check": "npm run typecheck && npm test && npm run test:upstash"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.7.5"
  }
}
